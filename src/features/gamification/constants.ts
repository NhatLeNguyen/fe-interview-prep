/** Hằng số Gamification (không import React). */

/** Mỗi level cần bấy nhiêu XP. Quy tắc XP nằm ở DB (function activity_xp). */
export const XP_PER_LEVEL = 100;

/** Số dòng tối đa của bảng xếp hạng (khớp giới hạn RPC leaderboard_anon). */
export const LEADERBOARD_LIMIT = 10;
