export const QUIZ_COUNT_OPTIONS = [5, 10, 20] as const;
export const QUIZ_DEFAULT_COUNT = 10;
export const QUIZ_MIN_COUNT = 1;
export const QUIZ_MAX_COUNT = 30;

/** Sentinel "tất cả" cho bộ lọc quiz (không đưa vào query). */
export const QUIZ_ALL = "all";
